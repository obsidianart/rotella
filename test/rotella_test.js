var expect = chai.expect,
	should = chai.should(),
	assert = chai.assert;

describe('rotella', function(){
	describe('#indexOf()', function(){
		it('should return -1 when the value is not present', function(){
			expect(true).to.be.ok;
			
		})
	})
})